# Dependencies
node_modules/

# Local HTTPS certificates for the ZET dev server.
# key.pem is a PRIVATE KEY — never commit it. Both are regenerated by the
# Zoho Extension Toolkit on a fresh checkout.
*.pem

# ZET build output
dist/
*.zip

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
